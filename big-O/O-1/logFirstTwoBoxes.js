// It doesn't matter if you do one or 10 operations at a time
// O(2) or O(N) will always be reduced to O(1) for representation purposes
const compressFirstBox = boxes => {
  console.log(boxes[0])
  console.log(boxes[1])
}